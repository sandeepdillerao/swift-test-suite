export enum TestRunStatus {
  ACTIVE = 'active',
  EXECUTING = 'executing',
  COMPLETED = 'completed',
  ARCHIVED = 'archived',
}

export enum ExecutionMode {
  MANUAL = 'manual',
  AUTOMATED = 'automated',
}
